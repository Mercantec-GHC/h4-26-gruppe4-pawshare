meta {
  name: delete /api/AnimalType/:id
  type: http
  seq: 5
  tags: [
    AnimalType
  ]
}

delete {
  url: {{baseUrl}}/api/AnimalType/:id
  body: none
  auth: inherit
}

params:path {
  id: 
}

example {
  name: 200 Response
  description: OK
  
  request: {
    url: {{baseUrl}}/api/AnimalType/:id
    method: DELETE
    mode: none
    params:path: {
      id: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
